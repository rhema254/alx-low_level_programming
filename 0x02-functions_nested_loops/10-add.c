#include "main.h"

/**
 * add - It sums any two integers given in the function
 * @a: the first operand in the operation
 * @b: the second operand in the operation
 * Return: the sum of the integers
 */

int add(int a, int b)
{	
	int sum = a + b;
	
	return (sum);
}
