#include "main.h"

/**
 * _abs - Prints the absolute value of any integer
 * @n: the integer to check
 * Return: the absolute value of int
 */

int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1);
	}
}
